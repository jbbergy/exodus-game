# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Claude Code local settings (personal permission overrides, not shared)
.claude/settings.local.json

# Raw audio masters (large, not needed at runtime — only the compressed
# exports in src/assets/audio/music are loaded by the game)
src/assets/audio/soundtrack/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
